# Pilot desktop releases — historical note

The earlier `0.1.0-pilot.1` release process has been superseded by the desktop alpha release process.

Use [`alpha.md`](alpha.md) for the current release gate, signing configuration, versioning rules and acceptance checks.

The old pilot build remains useful as release-engineering history, but it is not the current release target.
