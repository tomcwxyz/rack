# Public desktop-build configuration. These values are compiled into the Tauri web bundle and must not contain secrets.
VITE_RACK_SERVICE_URL=
VITE_NEON_AUTH_URL=

# Stable deployment-owned Rack model aliases used by managed Checks.
VITE_RACK_QUICK_MODEL_ALIAS=generator
VITE_RACK_RELIABLE_JUDGE_ALIAS=judge
